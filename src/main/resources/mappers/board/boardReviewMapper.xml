<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.model.dao.board.BoardReviewDao">
	<insert id="insert" parameterType="BoardReview" keyProperty="id" useGeneratedKeys="true">
		INSERT INTO boardReview
		(Board_id, User_id, content)
		VALUES
		(#{boardId}, #{userId}, #{content})
	</insert>
	<update id="update" parameterType="BoardReview">
		UPDATE boardReview
		SET
			Board_id = #{boardId},
			User_id = #{userId},
			content = #{content},
		WHERE id = #{id}
	</update>
	<delete id="delete" parameterType="int">
		DELETE FROM boardReview
		WHERE id = #{id}
	</delete>
	<select id="selectBoard" parameterType="int" resultType="BoardReview">
		SELECT * FROM boardReview WHERE Board_id=#{boardId}
	</select>
	<select id="selectUser" parameterType="String" resultType="BoardReview">
		SELECT * FROM boardReview WHERE User_id=#{userId}
	</select>
</mapper>
